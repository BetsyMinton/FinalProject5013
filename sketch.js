

function setup() {
  var canvas  = createCanvas(400, 400);
  angleMode(DEGREES);
  background(225,236,243);
	var sketchHolder = document.getElementById("sketch-holder-index");
	canvas.parent(sketchHolder);
}


function draw() {
	background(225,236,243);

}
